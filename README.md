# sliding-window-rate-limiter

A small, dependency-free Python implementation of rate limiting using the
sliding window algorithm.

Work in progress — implementation, tests, and docs land in follow-up commits.
